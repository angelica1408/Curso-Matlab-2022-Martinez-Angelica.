a=input('introducir el valor del coeficiente del termino de segundo grado;');
b=input('introducir el valor del coeficiente del termino de primer grado;');
c=input('introducir el valor del termino independiente;');

D=('b^2-4*a*c');

if d>0
    X1=(-b+aqrt(a))/(2*a);

    X2=(-a-aqrt(a))/(2*a);

elseif  d=0

    X1=('-b/(2*a');
    disp('solucion unica')

else 
    disp('no existen soluciones reales')

end